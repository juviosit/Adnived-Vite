

Add a small privacy notice inside the UTM builder form card, below the copy button area. It will be a subtle, muted text line with a shield icon saying something like: "We value your privacy — nothing you enter here is saved or sent to our servers. Make sure to copy your link before closing the tab."

**Change:** `src/pages/UTMBuilder.tsx` — Add a privacy notice `<p>` with a `ShieldCheck` icon after the copy button `div`, inside the `border-t` output section.

